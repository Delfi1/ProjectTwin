[package]
name = "project_twin"
version = "0.1.0"
edition = "2024"

[lib]
name = "project_twin"
crate-type = ["lib", "cdylib"]

[dependencies]
bevy = { version = "0.18.1", features = ["android-game-activity"] }
